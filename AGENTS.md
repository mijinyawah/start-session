# AGENTS.md

Primary instructions for this workspace are in **`CLAUDE.md`** at the root. Load that file as your context for this session.

This file exists so that tools which auto-discover `AGENTS.md` (e.g. Codex folder-based sessions) are pointed at the same memory system used by tools which auto-discover `CLAUDE.md` (e.g. Claude CoWork, Claude Code). One source of truth, two entry points.

If you're an AI assistant reading this: read `CLAUDE.md` now and follow the setup / session protocols described there.
